import "features/claims/newClaim/NewClaim.scss";

// Placeholder — the new-claim form is a separate future task.
const NewClaim = () => {
  return (
    <div className="new-claim">
      <h1>New claim</h1>
    </div>
  );
};

export default NewClaim;
